CREATE PROCEDURE spBuscarID

	@ID INT,
	@MSG VARCHAR(50) OUTPUT

AS
BEGIN

	IF EXISTS (SELECT * FROM CONTATOS WHERE ID = @ID)
	BEGIN
	SELECT * FROM CONTATOS WHERE ID = @ID
	RETURN
	END

	ELSE
	BEGIN
	SET @MSG = 'ID não encontrado'
	RETURN
	END

END